version: '3.8'

services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=prismo
    ports:
      - '5432:5432'
    volumes:
      - prismo-db-data:/var/lib/postgresql/data
    networks:
      - prismo-net

networks:
  prismo-net:
    driver: overlay
    attachable: true
    driver_opts:
      encrypted: 'true'

volumes:
  prismo-db-data:
