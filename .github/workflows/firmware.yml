name: Firmware merge requset pipeline

on:
  pull_request:
    paths:
      - "firmware/**"

jobs:
  firmware-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ruff
        run: pip install ruff

      - name: Lint firmware
        run: ruff check firmware/
