name: Firmware Lint

on:
  pull_request:
    paths:
      - "firmware/**"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ruff
        run: pip install ruff

      - name: Lint firmware
        run: ruff check firmware/
